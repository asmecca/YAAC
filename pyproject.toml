[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "YAAC"
version = "0.1.0"
description = "YAAC analysis utilities"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
